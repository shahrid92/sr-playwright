architecture: standalone

auth:
  enabled: false

master:
  persistence:
    enabled: false
  service:
    type: ClusterIP
    ports:
      redis: 6379
  resources:
    requests:
      memory: 128Mi
      cpu: 50m
    limits:
      memory: 256Mi
      cpu: 200m

replica:
  replicaCount: 0